~~~
Preferences -> workspace -> Refresh....  : 자바 바로 업데이트;
※ 일반논리 - " short circuit logic " ;;
JVM : 자바를 구동하는 가상머신;
자바는 서버와 운영체재로부터 종속적이지 않습니다. ( 자유롭다.);;
※LABEL -> LABEL: 
예외 처리 ->finally 무조건 실행하는 문장; ->커넥션 닫기?

클래스 - 서로 다른 자료형 여러 개를 하나로 묶는거;
구조체!? ㅎㅎㅎ
this(), this 구별하기 -> 함수는 다른생성자를 호출하는;
static -> 객체생성없이 클래스명으로 가지고 쓸수 잇다. Math.random()
1. abstract - 미완성
2. final - 변경불가 
3. interface -  구조(틀)? 안이 비워있는....
 -> 생성자 없구 모든 메소드가 추상 메소드;  public static final 변수;
 -> 부모역할 (implements) 로 상속관계;
 ->연결고리 (디비연결, 연결,통로) + 연결;

         19/04/30
   // String값을 int형의 값으로 바꾸는 방법
        int numInt = Integer.parseInt(numStr);
        System.out.println(numInt);
        
        // int형의 값을 String으로 바꾸는 방법
        String numStr2 = String.valueOf(numInt);
        System.out.println(numStr2);
~~~